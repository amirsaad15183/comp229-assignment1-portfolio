# Assignment 3 Plan

## Assignment summary
- Build on Assignment 2 and finish the full stack portfolio.
- Connect the React frontend to the backend APIs.
- Add frontend forms with state management.
- Add frontend CRUD behavior based on user roles.
- Support authentication, sign in, sign out, and protected routes.
- Hardcode one admin user in MongoDB.
- Ensure the full application works end to end and is error free.

## Extracted assignment steps

### Part I - Backend authentication
1. Complete backend authentication if anything is still missing from Assignment 2.
2. Create or confirm the sign in route.
3. Create or confirm the sign out route.
4. Create or confirm JWT-based authentication logic.
5. Create or confirm protected routes.
6. Test the API routes.

### Part II - Frontend integration
7. Create a `SignUp` form in React with state management.
8. Create a `SignIn` form in React with state management.
9. Create an `Education` or `Qualification` form in React with state management.
10. Create a `Project` form in React with state management.
11. Update the existing `Contact` form with React state management.
12. Integrate backend APIs into the frontend.
13. Perform frontend CRUD operations for Contact, Qualification, Project, and related objects.
14. Add authentication and user roles:
    - Admin can create, update, and delete.
    - User can only view/read.
15. Hardcode the admin credentials in MongoDB.
16. Allow normal users to sign up and sign in with their own credentials.

### Part III - Final application
17. Make sure frontend entries are stored in MongoDB.
18. Make sure frontend and backend communicate correctly.
19. Make sure the full stack portfolio works without errors.
20. Push updated code to GitHub.

## What Codex should do
- Review the current Assignment 2 project structure.
- Implement any missing backend authentication pieces.
- Add or update frontend pages, forms, routes, and components.
- Add state management for all required forms.
- Connect the frontend to backend APIs.
- Implement frontend CRUD flows.
- Add role-based UI behavior for admin vs normal user.
- Prepare a safe way to create the admin user record if needed.
- Update project files and help keep the repo organized.
- Help verify functionality locally.

## What Amir should do
- Take the required screenshots for submission.
- Confirm the admin email and password to use for the hardcoded admin account.
- If required by the instructor, manually verify the admin record in MongoDB Atlas.
- Push final screenshots into the Word document.
- Create the final zip file and submit it.

## Suggested user to-do list
- Take screenshot of working sign up form.
- Take screenshot of working sign in form.
- Take screenshot of working contact form.
- Take screenshot of working qualification form.
- Take screenshot of working project form.
- Take screenshot(s) showing CRUD from the frontend.
- Take screenshot(s) showing admin role behavior.
- Take screenshot(s) showing user read-only behavior.
- Take screenshot(s) showing saved data in MongoDB if needed.
- Take screenshot(s) showing the working full stack app.
- Export or save the final Word document for submission.

## Recommended build order
1. Audit the current Assignment 2 codebase.
2. Confirm backend auth pieces.
3. Add signup/signin frontend.
4. Connect auth to backend.
5. Add qualification/project/contact forms.
6. Connect forms to APIs.
7. Add frontend CRUD views.
8. Add admin/user role behavior.
9. Create or seed the admin user.
10. Test locally and collect screenshots.
